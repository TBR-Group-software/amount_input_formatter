name: amount_input_formatter
description: "A minimalistic and configurable number formatter"
version: 0.1.0
repository: https://github.com/TBR-Group-software/amount_input_formatter
issue_tracker: https://github.com/TBR-Group-software/amount_input_formatter/issues

environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^7.0.0
